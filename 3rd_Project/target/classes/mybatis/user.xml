<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sqlField_user">
	<select id="didYouJoin" resultType="boolean">
		select count(*) from users
		where u_id=#{u_id}
	</select>
	<insert id="register">
		insert into users
<<<<<<< HEAD
		values(#{u_id}, #{u_name}, #{u_password}, #{u_phone}, #{u_address}, 0, '미정', #{u_jointype}, default, #{u_color} )
=======
		values(#{u_id}, #{u_name}, #{u_password}, #{u_phone}, #{u_address}, 0, '미정', #{u_jointype}, default, #{u_color})
>>>>>>> b76f832404100d759d025f14781ac9d3afc5fa51
	</insert>
	<select id="getUserById" resultType="Users">
		select * from users
		where u_id=#{u_id}
	</select>
	<update id="updatePassword">
		update users
		set u_password=#{new_password}
		where u_id=#{u_id}
	</update>
	<update id="updateColor">
		update users
		set u_color=#{personalColor}
		where u_id=#{u_id}
	</update>
	<select id="findId" resultType="Users">
		select u_id from users
		where u_name=#{u_name} and u_phone=#{u_phone}
	</select>
	<select id="findPw" resultType="String">
		select u_password from users
		where u_name=#{u_name} and u_id=#{u_id} and u_phone=#{u_phone}
	</select>
	<select id="updateUsers" resultType="int">
		update users
		set u_phone=#{u_phone}, u_address=#{u_address}, u_color=#{u_color}
		where u_id=#{u_id}
	</select>
	<delete id="deleteUsers">
		delete users
		where u_id=#{u_id}
	</delete>
	
</mapper>