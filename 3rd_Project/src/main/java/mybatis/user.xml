<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sqlField_user">
	<select id="didYouJoin" resultType="boolean">
		select count(*) from users
		where u_id=#{u_id}
	</select>
	<insert id="register">
		insert into users
<<<<<<< HEAD
		values(#{u_id}, #{u_name}, #{u_password}, #{u_phone}, #{u_address}, 0, '미정', #{u_jointype}, default, #{u_color}, #{u_email}, '', '')
=======
		values(#{u_id}, #{u_name}, #{u_password}, #{u_phone}, #{u_address}, 
		0, '미정', #{u_jointype}, default, 
		#{u_color}, #{u_email}, #{u_profileimg}, #{u_intro} )
>>>>>>> branch 'master' of https://github.com/BeTheBestSY/3rd_Project.git
	</insert>
	<select id="getUserById" resultType="Users">
		select * from users
		where u_id=#{u_id}
	</select>
	<select id="findId" resultType="Users">
		select u_id from users
		where u_name=#{u_name} and u_phone=#{u_phone}
	</select>
	<select id="findPw" resultType="String">
		select u_password from users
		where u_name=#{u_name} and u_id=#{u_id} and u_phone=#{u_phone}
	</select>
	<insert id="insertFeedback">
		insert into feedback values(feedback_seq.nextval, #{u_id}, #{u_jointype}, #{fb_content}, #{fb_requestApi})
	</insert>
	<update id="updateUsers">
		update users
		set u_phone=#{u_phone}, u_address=#{u_address}, u_color=#{u_color}
		where u_id=#{u_id}
	</update>
		<update id="updatePassword">
		update users
		set u_password=#{new_password}
		where u_id=#{u_id}
	</update>
	<update id="updateColor">
		update users
		set u_color=#{personalColor}
		where u_id=#{u_id}
	</update>
	<delete id="deleteUsers">
		delete users
		where u_id=#{u_id}
	</delete>
	
</mapper>